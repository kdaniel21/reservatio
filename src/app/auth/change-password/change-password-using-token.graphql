mutation changePasswordUsingToken($token: String!, $password: String!, $passwordConfirm: String!) {
  changePasswordUsingToken(passwordResetToken: $token, password: $password, passwordConfirm: $passwordConfirm) {
    message
  }
}
